# 前端开发指南

## 📚 导航

[← 后端开发指南](./02-后端开发指南.md) | [数据大屏开发 →](./04-数据大屏开发.md)

---

## 🎯 前端架构概述

NO-CRM 前端基于 Vue 3 + TypeScript + Vite 构建，使用 TDesign 作为 UI 组件库。

### 技术栈特点

- **Vue 3**：使用 Composition API，代码更清晰
- **TypeScript**：类型安全，减少错误
- **Vite**：极速的开发体验
- **TDesign**：腾讯开源的企业级组件库
- **Pinia**：轻量级状态管理
- **Vue Router**：路由管理

---

## 📁 项目结构

```
frontend/src/
├── api/               # API 接口层
│   ├── index.ts      # Axios 配置
│   ├── auth.ts       # 认证接口
│   ├── customer.ts   # 客户接口
│   ├── lead.ts       # 线索接口
│   ├── activity.ts   # 跟进记录接口
│   ├── task.ts       # 任务接口
│   └── statistics.ts # 统计接口
│
├── views/            # 页面组件
│   ├── Login.vue     # 登录页
│   ├── Dashboard.vue # 数据大屏
│   ├── Customers.vue # 客户管理
│   ├── Leads.vue     # 线索管理
│   ├── Activities.vue# 跟进记录
│   └── Tasks.vue     # 任务管理
│
├── layouts/          # 布局组件
│   └── MainLayout.vue# 主布局
│
├── stores/           # 状态管理
│   └── auth.ts       # 认证状态
│
├── router/           # 路由配置
│   └── index.ts
│
├── App.vue           # 根组件
├── main.ts           # 入口文件
└── style.css         # 全局样式
```

---

## 🚀 Vue 3 基础

### Composition API

Vue 3 推荐使用 `<script setup>` 语法，更简洁。

```vue
<template>
  <div>
    <h1>{{ title }}</h1>
    <button @click="increment">Count: {{ count }}</button>
  </div>
</template>

<script setup lang="ts">
import { ref } from 'vue'

// 响应式数据
const title = ref('Hello Vue 3')
const count = ref(0)

// 方法
const increment = () => {
  count.value++
}
</script>
```

### 常用 API

#### ref - 基本类型响应式

```typescript
const count = ref(0)
const name = ref('张三')
const isVisible = ref(false)

// 访问/修改值需要 .value
console.log(count.value)  // 0
count.value = 1
```

#### reactive - 对象响应式

```typescript
const state = reactive({
  count: 0,
  name: '张三',
  user: {
    age: 25
  }
})

// 直接访问属性
console.log(state.count)  // 0
state.count = 1
```

#### computed - 计算属性

```typescript
const firstName = ref('张')
const lastName = ref('三')

const fullName = computed(() => {
  return firstName.value + lastName.value
})

console.log(fullName.value)  // '张三'
```

#### watch - 侦听器

```typescript
const count = ref(0)

watch(count, (newVal, oldVal) => {
  console.log(`count 从 ${oldVal} 变为 ${newVal}`)
})

// 监听多个值
watch([count, name], ([newCount, newName], [oldCount, oldName]) => {
  // ...
})
```

#### onMounted - 生命周期

```typescript
onMounted(() => {
  console.log('组件挂载完成')
  loadData()  // 加载数据
})

onUnmounted(() => {
  console.log('组件卸载')
  cleanup()  // 清理工作
})
```

---

## 🌐 API 调用

### Axios 配置

```typescript
// api/index.ts
import axios from 'axios'
import router from '../router'

const api = axios.create({
  baseURL: '/api',
  timeout: 10000,
})

// 请求拦截器 - 添加 Token
api.interceptors.request.use(config => {
  const token = localStorage.getItem('token')
  if (token) {
    config.headers.Authorization = `Bearer ${token}`
  }
  return config
})

// 响应拦截器 - 处理错误
api.interceptors.response.use(
  response => response.data,
  error => {
    if (error.response?.status === 401) {
      // 登录过期，跳转到登录页
      localStorage.removeItem('token')
      router.push('/login')
    }
    return Promise.reject(error)
  }
)

export default api
```

### 定义接口

```typescript
// api/customer.ts
import api from './index'

export interface Customer {
  id: string
  name: string
  phone: string
  company?: string
  // ...
}

export interface CreateCustomerData {
  name: string
  phone: string
  company?: string
  // ...
}

export const customerApi = {
  // 获取列表
  getAll(keyword?: string) {
    return api.get<any, Customer[]>('/customers', { params: { keyword } })
  },
  
  // 获取单个
  getOne(id: string) {
    return api.get<any, Customer>(`/customers/${id}`)
  },
  
  // 创建
  create(data: CreateCustomerData) {
    return api.post<any, Customer>('/customers', data)
  },
  
  // 更新
  update(id: string, data: Partial<CreateCustomerData>) {
    return api.patch<any, Customer>(`/customers/${id}`, data)
  },
  
  // 删除
  delete(id: string) {
    return api.delete(`/customers/${id}`)
  }
}
```

### 在组件中使用

```vue
<script setup lang="ts">
import { ref, onMounted } from 'vue'
import { customerApi, type Customer } from '@/api/customer'
import { MessagePlugin } from 'tdesign-vue-next'

const customers = ref<Customer[]>([])
const loading = ref(false)

// 加载数据
const loadCustomers = async () => {
  loading.value = true
  try {
    customers.value = await customerApi.getAll()
  } catch (error) {
    MessagePlugin.error('加载失败')
  } finally {
    loading.value = false
  }
}

// 创建客户
const handleCreate = async (data: CreateCustomerData) => {
  try {
    await customerApi.create(data)
    MessagePlugin.success('创建成功')
    loadCustomers()  // 重新加载列表
  } catch (error) {
    MessagePlugin.error('创建失败')
  }
}

onMounted(() => {
  loadCustomers()
})
</script>
```

---

## 🎨 TDesign 组件使用

### 表格 Table

```vue
<template>
  <t-table
    :data="customers"
    :columns="columns"
    :loading="loading"
    row-key="id"
    stripe
    hover
  >
    <!-- 自定义列 -->
    <template #tags="{ row }">
      <t-tag v-for="tag in row.tags" :key="tag">{{ tag }}</t-tag>
    </template>
    
    <!-- 操作列 -->
    <template #action="{ row }">
      <t-link theme="primary" @click="handleEdit(row)">编辑</t-link>
      <t-popconfirm content="确定删除吗？" @confirm="handleDelete(row.id)">
        <t-link theme="danger">删除</t-link>
      </t-popconfirm>
    </template>
  </t-table>
</template>

<script setup lang="ts">
const columns = [
  { colKey: 'name', title: '姓名', width: 120 },
  { colKey: 'phone', title: '电话', width: 140 },
  { colKey: 'company', title: '公司', width: 150 },
  { colKey: 'tags', title: '标签', width: 200 },
  { colKey: 'action', title: '操作', width: 150, fixed: 'right' }
]
</script>
```

### 表单 Form

```vue
<template>
  <t-form :data="formData" @submit="handleSubmit" label-align="right" :label-width="80">
    <t-form-item label="姓名" name="name">
      <t-input v-model="formData.name" placeholder="请输入姓名" />
    </t-form-item>
    
    <t-form-item label="电话" name="phone">
      <t-input v-model="formData.phone" placeholder="请输入电话" />
    </t-form-item>
    
    <t-form-item label="标签" name="tags">
      <t-select v-model="formData.tags" multiple creatable>
        <t-option value="重点客户" label="重点客户" />
        <t-option value="意向客户" label="意向客户" />
      </t-select>
    </t-form-item>
    
    <t-form-item>
      <t-button theme="primary" type="submit">提交</t-button>
    </t-form-item>
  </t-form>
</template>

<script setup lang="ts">
const formData = ref({
  name: '',
  phone: '',
  tags: []
})

const handleSubmit = () => {
  console.log('表单数据:', formData.value)
}
</script>
```

### 对话框 Dialog

```vue
<template>
  <t-button @click="visible = true">打开对话框</t-button>
  
  <t-dialog
    v-model:visible="visible"
    header="编辑客户"
    width="600px"
    @confirm="handleConfirm"
  >
    <t-form :data="formData">
      <!-- 表单内容 -->
    </t-form>
  </t-dialog>
</template>

<script setup lang="ts">
const visible = ref(false)
const formData = ref({})

const handleConfirm = () => {
  // 提交逻辑
  visible.value = false
}
</script>
```

### 消息提示 Message

```typescript
import { MessagePlugin } from 'tdesign-vue-next'

// 成功提示
MessagePlugin.success('操作成功')

// 错误提示
MessagePlugin.error('操作失败')

// 警告提示
MessagePlugin.warning('请填写必填信息')

// 普通提示
MessagePlugin.info('暂无数据')
```

### 按钮 Button

```vue
<template>
  <!-- 主要按钮 -->
  <t-button theme="primary">主要按钮</t-button>
  
  <!-- 危险按钮 -->
  <t-button theme="danger">删除</t-button>
  
  <!-- 带图标的按钮 -->
  <t-button theme="primary">
    <template #icon><t-icon name="add" /></template>
    新建
  </t-button>
  
  <!-- 加载状态 -->
  <t-button theme="primary" :loading="loading">提交</t-button>
</template>
```

---

## 🗺️ 路由管理

### 路由配置

```typescript
// router/index.ts
import { createRouter, createWebHistory } from 'vue-router'

const router = createRouter({
  history: createWebHistory(),
  routes: [
    {
      path: '/login',
      name: 'Login',
      component: () => import('@/views/Login.vue')
    },
    {
      path: '/',
      component: () => import('@/layouts/MainLayout.vue'),
      redirect: '/dashboard',
      children: [
        {
          path: 'dashboard',
          name: 'Dashboard',
          component: () => import('@/views/Dashboard.vue')
        },
        {
          path: 'customers',
          name: 'Customers',
          component: () => import('@/views/Customers.vue')
        }
      ]
    }
  ]
})

export default router
```

### 路由守卫

```typescript
// 全局前置守卫
router.beforeEach((to, from, next) => {
  const authStore = useAuthStore()
  
  // 未登录且访问非登录页，跳转到登录页
  if (to.path !== '/login' && !authStore.isAuthenticated()) {
    next('/login')
  }
  // 已登录且访问登录页，跳转到首页
  else if (to.path === '/login' && authStore.isAuthenticated()) {
    next('/')
  }
  else {
    next()
  }
})
```

### 编程式导航

```typescript
import { useRouter } from 'vue-router'

const router = useRouter()

// 跳转到指定路由
router.push('/customers')

// 带参数跳转
router.push({
  name: 'CustomerDetail',
  params: { id: '123' }
})

// 返回上一页
router.back()

// 替换当前路由（不会留下历史记录）
router.replace('/login')
```

---

## 📦 状态管理 Pinia

### 定义 Store

```typescript
// stores/auth.ts
import { defineStore } from 'pinia'
import { ref } from 'vue'

export const useAuthStore = defineStore('auth', () => {
  // 状态
  const user = ref<User | null>(null)
  const token = ref<string | null>(null)

  // 初始化（从 localStorage 读取）
  const initAuth = () => {
    const savedToken = localStorage.getItem('token')
    const savedUser = localStorage.getItem('user')
    
    if (savedToken && savedUser) {
      token.value = savedToken
      user.value = JSON.parse(savedUser)
    }
  }

  // 登录
  const login = async (email: string, password: string) => {
    const response = await authApi.login({ email, password })
    user.value = response.user
    token.value = response.token
    
    localStorage.setItem('token', response.token)
    localStorage.setItem('user', JSON.stringify(response.user))
  }

  // 登出
  const logout = () => {
    user.value = null
    token.value = null
    localStorage.removeItem('token')
    localStorage.removeItem('user')
  }

  // 计算属性
  const isAuthenticated = () => {
    return !!token.value
  }

  return {
    user,
    token,
    initAuth,
    login,
    logout,
    isAuthenticated
  }
})
```

### 使用 Store

```vue
<script setup lang="ts">
import { useAuthStore } from '@/stores/auth'

const authStore = useAuthStore()

// 访问状态
console.log(authStore.user)
console.log(authStore.token)

// 调用方法
authStore.login('email@example.com', 'password')
authStore.logout()

// 使用计算属性
if (authStore.isAuthenticated()) {
  console.log('已登录')
}
</script>
```

---

## 🎨 样式开发

### Scoped CSS

```vue
<style scoped>
.page-header {
  margin-bottom: 24px;
}

.stat-card {
  background: #fff;
  border-radius: 8px;
  padding: 20px;
}
</style>
```

### CSS 变量

```css
:root {
  --primary-color: #0052d9;
  --success-color: #00a870;
  --warning-color: #e37318;
  --danger-color: #e34d59;
}

.button-primary {
  background: var(--primary-color);
}
```

### 动画

```css
/* 淡入动画 */
@keyframes fadeIn {
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}

.fade-in {
  animation: fadeIn 0.3s ease-in;
}

/* 滑入动画 */
@keyframes slideIn {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.slide-in {
  animation: slideIn 0.5s ease-out;
}
```

---

## 🎯 开发新页面

### 步骤示例：创建"报价单"页面

#### 1. 定义 API 接口

```typescript
// api/quotation.ts
export interface Quotation {
  id: string
  customerId: string
  amount: number
  items: QuotationItem[]
  status: 'draft' | 'sent' | 'accepted' | 'rejected'
  createdAt: string
}

export const quotationApi = {
  getAll() {
    return api.get<any, Quotation[]>('/quotations')
  },
  
  create(data: CreateQuotationData) {
    return api.post<any, Quotation>('/quotations', data)
  }
  
  // ... 其他方法
}
```

#### 2. 创建页面组件

```vue
<!-- views/Quotations.vue -->
<template>
  <div class="quotations-page">
    <div class="page-header">
      <t-button theme="primary" @click="showCreateDialog = true">
        <template #icon><t-icon name="add" /></template>
        新建报价单
      </t-button>
    </div>

    <t-table
      :data="quotations"
      :columns="columns"
      :loading="loading"
      row-key="id"
    >
      <!-- 自定义列模板 -->
      <template #status="{ row }">
        <t-tag :theme="getStatusTheme(row.status)">
          {{ getStatusLabel(row.status) }}
        </t-tag>
      </template>
      
      <template #action="{ row }">
        <t-link theme="primary" @click="handleEdit(row)">编辑</t-link>
      </template>
    </t-table>

    <!-- 创建对话框 -->
    <t-dialog v-model:visible="showCreateDialog" header="新建报价单">
      <t-form :data="formData">
        <!-- 表单字段 -->
      </t-form>
    </t-dialog>
  </div>
</template>

<script setup lang="ts">
import { ref, onMounted } from 'vue'
import { quotationApi, type Quotation } from '@/api/quotation'
import { MessagePlugin } from 'tdesign-vue-next'

const quotations = ref<Quotation[]>([])
const loading = ref(false)
const showCreateDialog = ref(false)

const columns = [
  { colKey: 'id', title: 'ID', width: 100 },
  { colKey: 'customerId', title: '客户', width: 150 },
  { colKey: 'amount', title: '金额', width: 120 },
  { colKey: 'status', title: '状态', width: 100 },
  { colKey: 'action', title: '操作', width: 150 }
]

const loadQuotations = async () => {
  loading.value = true
  try {
    quotations.value = await quotationApi.getAll()
  } catch (error) {
    MessagePlugin.error('加载失败')
  } finally {
    loading.value = false
  }
}

const getStatusLabel = (status: string) => {
  const labels: Record<string, string> = {
    draft: '草稿',
    sent: '已发送',
    accepted: '已接受',
    rejected: '已拒绝'
  }
  return labels[status] || status
}

const getStatusTheme = (status: string) => {
  const themes: Record<string, string> = {
    draft: 'default',
    sent: 'primary',
    accepted: 'success',
    rejected: 'danger'
  }
  return themes[status] || 'default'
}

onMounted(() => {
  loadQuotations()
})
</script>

<style scoped>
.quotations-page {
  background: #fff;
  padding: 24px;
  border-radius: 4px;
}

.page-header {
  margin-bottom: 24px;
}
</style>
```

#### 3. 添加路由

```typescript
// router/index.ts
{
  path: 'quotations',
  name: 'Quotations',
  component: () => import('@/views/Quotations.vue')
}
```

#### 4. 添加菜单

```vue
<!-- layouts/MainLayout.vue -->
<t-menu-item value="quotations">
  <template #icon><t-icon name="file" /></template>
  报价单管理
</t-menu-item>
```

---

## 🐛 调试技巧

### 1. Vue DevTools

安装 Vue DevTools 浏览器插件，可以查看：
- 组件树结构
- 组件状态（data）
- Pinia store 状态
- 路由信息
- 性能分析

### 2. Console 调试

```typescript
// 查看变量
console.log('customers:', customers.value)

// 查看对象
console.table(customers.value)

// 断点调试
debugger

// 性能测试
console.time('loadData')
await loadData()
console.timeEnd('loadData')
```

### 3. 网络请求调试

打开浏览器开发者工具 → Network 标签页，可以查看：
- 请求 URL
- 请求方法
- 请求头（Headers）
- 请求体（Payload）
- 响应数据（Response）

---

## 💡 最佳实践

### 1. 组件命名

```
✅ 推荐：使用 PascalCase
CustomerList.vue
UserProfile.vue

❌ 不推荐：
customerList.vue
user-profile.vue
```

### 2. Props 定义

```typescript
// ✅ 推荐：使用 TypeScript
interface Props {
  customer: Customer
  isEditing?: boolean
}

const props = defineProps<Props>()

// ❌ 不推荐
defineProps(['customer', 'isEditing'])
```

### 3. Emit 定义

```typescript
// ✅ 推荐
const emit = defineEmits<{
  (e: 'update', value: Customer): void
  (e: 'delete', id: string): void
}>()

emit('update', customer)

// ❌ 不推荐
const emit = defineEmits(['update', 'delete'])
```

### 4. 条件渲染

```vue
<!-- ✅ 推荐：使用 v-if/v-else -->
<div v-if="customers.length > 0">
  <customer-list :data="customers" />
</div>
<div v-else>
  <t-empty description="暂无数据" />
</div>

<!-- ❌ 不推荐：嵌套过深 -->
<div v-if="!loading">
  <div v-if="customers">
    <div v-if="customers.length > 0">
      ...
    </div>
  </div>
</div>
```

### 5. 性能优化

```vue
<!-- 使用 v-show 而不是 v-if（频繁切换） -->
<div v-show="isVisible">内容</div>

<!-- 列表渲染添加 key -->
<div v-for="item in list" :key="item.id">
  {{ item.name }}
</div>

<!-- 大列表虚拟滚动 -->
<virtual-list :data="bigList" :item-height="50" />
```

---

## 📚 常用命令

```bash
# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 预览构建结果
npm run preview

# 类型检查
vue-tsc --noEmit

# 代码格式化
npm run format
```

---

## 🔗 下一步

- 了解 [数据大屏开发](./04-数据大屏开发.md)
- 查看 [常见问题FAQ](./05-常见问题FAQ.md)
- 返回 [后端开发指南](./02-后端开发指南.md)
